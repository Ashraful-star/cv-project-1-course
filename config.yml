title: CV project@1
description: 'This is my first cv using html css float property '
template:
  repo: cv-project@1-template
  name: cv-project@1
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
